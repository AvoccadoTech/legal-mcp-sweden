__pycache__/
*.py[cod]
.venv/
venv/
build/
dist/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Watcher state. The ledger records which legal positions a firm tracks and
# which knowledge-base files depend on them — that list is a map of the firm's
# matters. It belongs on the firm's disk and nowhere else.
.lifos/
*-state.json
